# backend.env.example  (copia a .env y rellena)

# ── App ──────────────────────────────
APP_NAME=DBA Assistant
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# ── SQL Server ───────────────────────
SQL_SERVER_HOST=sqlserver
SQL_SERVER_PORT=1433
SQL_SERVER_USER=sa
SQL_SERVER_PASSWORD=YourStrong!Passw0rd
SQL_SERVER_DATABASE=DBAAssistant

# ── MongoDB ──────────────────────────
MONGODB_URI=mongodb://mongo:27017
MONGODB_DATABASE=DBAAssistant

# ── Redis ────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── Autenticación (Sprint 2) ─────────
# Modo: "local" (JWT propio, sin Auth0) o "auth0" (RS256 + JWKS)
AUTH_MODE=local
SECRET_KEY=genera-una-clave-larga-y-aleatoria
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Auth0 (rellenar cuando crees el tenant y pongas AUTH_MODE=auth0)
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_AUDIENCE=

# ── (Sprint 3+) Claude — placeholders ─
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-3-5-sonnet-20241022
CLAUDE_MAX_TOKENS=1024