# backend/requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Bases de datos
sqlalchemy==2.0.36
pyodbc==5.2.0            # Driver SQL Server (requiere ODBC Driver 18)
pymongo==4.10.1
redis==5.2.1

# Utilidades
python-dotenv==1.0.1
httpx==0.28.1

# Control de host a nivel SO vía WinRM (Sprint 10.1): iniciar el servicio de
# SQL Server cuando está caído. requests_ntlm habilita el transport NTLM.
pywinrm==0.4.3
requests_ntlm==1.3.0

# IA / Claude (Sprint 3)
anthropic==0.40.0

# Autenticación / Seguridad (Sprint 2)
PyJWT[crypto]==2.10.1     # JWT: HS256 (local) y RS256 + JWKS (Auth0)
passlib[bcrypt]==1.7.4    # Hash de contraseñas
bcrypt==4.0.1             # Pinned: evita warning de passlib con bcrypt>=4.1
python-multipart==0.0.20  # Form data para OAuth2PasswordRequestForm

# Testing
pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0