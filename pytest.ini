[pytest]
pythonpath = backend
testpaths = tests/unit
markers =
    integration: requiere los servicios reales levantados (backend/SQL/Mongo/Redis)
addopts = --cov=app --cov-report=term-missing:skip-covered
